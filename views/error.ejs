<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SISTA | FEB Universitas Hasanuddin</title>
  <link rel="icon" type="image/icon type" href="https://unhas.ac.id/v2/wp-content/uploads/2021/04/Logo-Unhas.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheets/error.css">
</head>

<body class="d-flex text-center text-white bg-dark">
  <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
    <header class="mb-auto">
      <div>
        <h3 class="float-md-start mb-0">SISTA</h3>
        <nav class="nav nav-masthead justify-content-center float-md-end">
          <a class="nav-link active" aria-current="page" href="*">ERROR</a>
          <% if (currentUser==null) { %>
            <a class="nav-link" aria-current="page" href="/">HOME</a>
            <% } else if(currentUser.role==="Student" ) { %>
              <a class="nav-link" aria-current="page" href="/student">DASHBOARD</a>
              <% } else if (currentUser.role==="Lecturer" ) { %>
                <a class="nav-link" aria-current="page" href="/lecturer">DASHBOARD</a>
                <% } else if (currentUser.role==="Admin" ) { %>
                  <a class="nav-link" aria-current="page" href="/admin">DASHBOARD</a>
                  <% } %>
      </div>
    </header>

    <main>
      <div class="row">
        <div class="main-body col-6 offset-3">
          <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading">
              <%= err.message %>
            </h4>
            <% if (process.env.NODE_ENV !=="production" ) { %>
              <p>
                <%= err.stack %>
              </p>
              <% } %>
          </div>
          <% if (currentUser==null) { %>
            <a class="btn btn-md btn-secondary font-weight-bold bg-white mb-2" aria-current="page" href="/">HOME</a>
            <% } else if(currentUser.role==="Student" ) { %>
              <a class="btn btn-md btn-secondary font-weight-bold bg-white mb-2" aria-current="page"
                href="/student">RETURN
                TO DASHBOARD</a>
              <% } else if (currentUser.role==="Lecturer" ) { %>
                <a class="btn btn-md btn-secondary font-weight-bold bg-white mb-2" aria-current="page"
                  href="/lecturer">RETURN TO DASHBOARD</a>
                <% } else if (currentUser.role==="Admin" ) { %>
                  <a class="btn btn-md btn-secondary font-weight-bold bg-white mb-2" aria-current="page"
                    href="/admin">RETURN TO DASHBOARD</a>
                  <% } %>
        </div>
      </div>
    </main>

    <footer class="mt-auto text-white-50">
      <p>SISTA | SISTEM TUGAS AKHIR</p>
      <p>HASANUDDIN UNIVERSITY</p>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
    crossorigin="anonymous"></script>
</body>

</html>