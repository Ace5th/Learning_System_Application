<% layout("layouts/boilerplateAdmins") %>

    <h1 class="page-title">Faq</h1>
    <div class="dropdown">
        <button class="btn btn-sm btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown"
            aria-expanded="false">
            &#8226;&#8226;&#8226;
        </button>
        <ul class="dropdown-menu dropdown-menu-dark">
            <li>
                <a class="dropdown-item" href="/admin/info-faqs">Previous Page</a>
            </li>
        </ul>
    </div>


    <% if (faq.files!=undefined) { %>
        <div class="row">
            <div class="col-6 offset-3">
                <div id="faqCarousel" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        <% faq.files.forEach((img, i)=> { %>
                            <div class="carousel-item <%= i === 0 ? 'active' : ''%> ">
                                <img class="d-block w-100" src="<%= img.url %> " alt="">
                            </div>
                            <% }) %>
                    </div>
                    <% if(faq.files.length> 1) {%>
                        <a class="carousel-control-prev" href="#faqCarousel" role="button" data-bs-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="sr-only"></span>
                        </a>
                        <a class="carousel-control-next" href="#faqCarousel" role="button" data-bs-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="sr-only"></span>
                        </a>
                        <% } %>
                </div>
            </div>
        </div>
        <% } %>

            <div class="row mt-5">
                <h4>Detail</h4>
            </div>

            <table class="table table-striped-columns">
                <thead>
                    <tr>
                        <th scope="col">Title</th>
                        <th scope="col">Content</th>
                        <th scope="col">Audience</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th scope="row">
                            <%= faq.title %>
                        </th>
                        <td>
                            <%= faq.content %>
                        </td>
                        <td>
                            <%= faq.audience %>
                        </td>
                        <td>
                            <div class="dropdown">
                                <button class="btn btn-sm btn-secondary dropdown-toggle" type="button"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                    &#8226;&#8226;&#8226;
                                </button>
                                <ul class="dropdown-menu dropdown-menu-dark">
                                    <li><a class="dropdown-item"
                                            href="/admin/info-faqs/<%= faq._id %>/edit-faq">Edit</a>
                                    </li>
                                    <li><a class="dropdown-item"
                                            href="/admin/info-faqs/<%= faq._id %>/delete-faq">Delete</a>
                                    </li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>