<% layout("layouts/boilerplateLecturers") %>

    <div class="container d-flex justify-content-center align-items-center my-5">
        <div class="row">
            <div class="col-md-6 col-xl-4">
                <div class="card shadow login-card">
                    <div class="login-logo">
                        <img class="card-img-top"
                            src="https://kompaspedia.kompas.id/wp-content/uploads/2020/08/logo_Universitas-Hasanudin.png"
                            alt="">
                    </div>
                    <div class="card-body">
                        <h5 class="text-center">Edit Profile</h5>
                        <form action="/lecturer/setting/profile?_method=PUT" method="POST" class="mb-4 validated-form"
                            novalidate>
                            <div class="mb-3">
                                <label class="form-label" for="username">NIM</label>
                                <input class="form-control" type="text" id="username" name="lecturer[username]"
                                    placeholder="NIM" value="<%= lecturer.username %>" autofocus required>
                                <div class="valid-feedback">
                                    Looks good!
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label" for="name">NAME</label>
                                <input class="form-control" type="text" id="name" name="lecturer[name]"
                                    placeholder="NAME" value="<%= lecturer.name %>" required>
                                <div class="valid-feedback">
                                    Looks good!
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label" for="email">E-MAIL</label>
                                <input class="form-control" type="email" id="email" name="lecturer[email]"
                                    placeholder="E-MAIL" value="<%= lecturer.email %>" required>
                                <div class="valid-feedback">
                                    Looks good!
                                </div>
                            </div>
                            <div class="input-group mb-3">
                                <label class="input-group-text" for="department">DEPARTMENT</label>
                                <select class="form-select" id="department" name="lecturer[department]">
                                    <option value="<%= lecturer.department %>" selected>
                                        <%= lecturer.department %>
                                    </option>
                                    <% for (let department of departments) { %>
                                        <option value="<%= department %>">
                                            <%= department %>
                                        </option>
                                        <% } %>
                                </select>
                                <div class="valid-feedback">
                                    Looks good!
                                </div>
                            </div>
                            <button class="btn btn-secondary btn-block">Edit Profile</button>
                        </form>
                        <a class="btn btn-sm btn-secondary mt-3" onclick="history.back()">&larr;
                            Previous Page</a>
                    </div>
                </div>
            </div>
        </div>